# Переменные окружения для CRM Monitor
# Скопировать в .env и заполнить реальными значениями

# Supabase URL (ваш проект Supabase)
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key (публичный ключ)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Примечание: эти переменные используются только на этапе сборки
# После сборки они будут встроены в JavaScript бандл
